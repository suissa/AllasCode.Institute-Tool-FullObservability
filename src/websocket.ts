import type {TelemetryRecord} from './types.js'; import type {ObservabilityExporter} from './exporter.js';
export interface WebSocketPeer{send(data:string):void;readyState:number}
export class WebSocketExporter implements ObservabilityExporter{private peers=new Set<WebSocketPeer>();addPeer(peer:WebSocketPeer){this.peers.add(peer);return()=>this.peers.delete(peer)}async export(records:readonly TelemetryRecord[]){for(const record of records){const data=JSON.stringify({protocol:'fullobservability.v1',signal:record.signal,emittedAtUnixNano:(BigInt(Date.now())*1000000n).toString(),payload:JSON.parse(JSON.stringify(record,(_k,v)=>typeof v==='bigint'?v.toString():v))});for(const peer of this.peers)if(peer.readyState===1)try{peer.send(data)}catch{}}}async forceFlush(){}async shutdown(){this.peers.clear()}}
